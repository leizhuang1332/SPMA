# Design: 企业级多源RAG智能问答系统（SPMA）— 全局概览

Generated by /office-hours on 2026-06-04
Branch: main
Repo: SPMA
Status: APPROVED
Mode: Intrapreneurship

> **文档导航：** 本文档是 SPMA 设计文档的全局入口，提供架构全景、决策背景、交付路线图和成功标准的高层视图。各模块的详细设计请参见对应文档：
> - [Supervisor Agent 设计](SPMA-design-01-supervisor-agent.md) — 意图分类、实体抽取、完备度评估、查询改写
> - [Doc Worker 设计](SPMA-design-02-doc-worker.md) — PRD 文档检索策略
> - [Code Worker 设计](SPMA-design-03-code-worker.md) — AST grep 结构化代码检索 + 多仓库路由
> - [SQL Worker 设计](SPMA-design-04-sql-worker.md) — Text-to-SQL、SQL Guard、正确性保障
> - [数据摄入管道设计](SPMA-design-05-data-ingestion.md) — 三源数据离线/异步摄入
> - [基础设施与运维设计](SPMA-design-06-infrastructure.md) — 技术选型、降级策略、安全、测试、部署
> - [5独立Agent架构设计](SPMA-design-07-agent-architecture.md) — Agent收敛契约、质量函数、状态管理、技术选型（CEO + Eng 双审查通过）

---

## Problem Statement

产品经理和开发工程师在企业内部面临严重的信息碎片化问题。三种核心数据来源——PRD 产品文档、开发代码、SQL 数据库——分散在不同的系统和工具中（Confluence/Wiki、IDE、数据库客户端），每次查询需要切换系统、手动搜索、人脑关联。这种"三系统切换"的模式每天浪费大量时间，导致：

- **产品经理**：查不到历史需求的完整上下文，做决策缺乏数据支撑
- **开发工程师**：故障定位时无法快速追溯代码到需求，查数据验证问题需要找 DBA 或手写 SQL
- **两拨人的共同痛点**：无法用自然语言直接查询业务数据

## Demand Evidence

- 具体的团队同事明确投诉过这个问题——有具体的人和场景，不是泛泛的"可能有用"
- 现状是多系统切换 + 人工搜索，每天切换 5-10+ 次
- 两拨用户群体都有明确的使用场景：PM 的需求溯源场景、开发的故障定位场景、共同的数据查询场景

## Status Quo

当前的"竞品"是碎片化的工具组合：

| 用户 | 现状 | 代价 |
|------|------|------|
| 产品经理 | Confluence/Wiki 搜索 PRD 文档 | 找不到历史版本、无法对比、需要人脑关联需求变更 |
| 开发工程师 | IDE grep 搜索代码 + 手动追踪需求 | 故障定位慢、新人上手周期长（依赖老员工口传） |
| 双方共同 | 找 DBA 或手写 SQL 查询数据 | 瓶颈在 DBA 带宽、非技术人员完全无法自助 |

信息获取依赖人际网络——新人入职要几个月才能独立工作。这是企业知识管理的典型失效模式。

## Target User & Narrowest Wedge

**核心用户画像：**
- 产品经理：需要需求溯源能力——某个功能为什么这么做？历史版本怎么演化的？这个改动影响了哪些下游？
- 开发工程师：需要快速定位能力——这个 bug 跟哪个需求相关？涉及哪些代码模块？影响了哪些数据表？

**Wedge：** 三源全覆盖 + 企业级工程化——用户明确要求不是 MVP 原型，而是从第一天就按生产标准设计的完整系统。对 intrapreneurship 场景来说，这是合理的：内部工具如果不可靠，用户不会用第二次。

## Constraints

- 企业内网部署，敏感数据不出企业边界。LLM 调用必须经过数据脱敏层（脱敏后可通过外网 API），或使用本地部署模型
- 三种数据源差异极大（自然语言文档、结构化代码、关系型数据库），不能用一套检索策略覆盖
- 内部用户对响应延迟敏感：P99 < 3 秒（单源查询）、P99 < 5 秒（跨源查询）
- 需要支持中文内容（PRD 文档、自然语言查询、中文代码注释）
- 系统可靠性决定用户信任——一次查询失败可能导致用户放弃使用

## Premises

1. **三种数据源需要三种不同的检索策略** — PRD 文档用语义搜索+关键词混合检索，代码用 AST 感知的结构化搜索，SQL 用 Schema 感知的 RAG 检索。不能用一套策略打天下。

2. **Supervisor-Worker 智能体架构** — 一个路由层理解用户意图，分发到专门的 Worker（文档 Worker / 代码 Worker / SQL Worker），各自独立检索，最后由合成层汇总。跨源查询是核心价值——不是锦上添花。

3. **四级降级保障** — 全功能 → LLM 降级（切换备用模型）→ 向量检索降级（纯关键词）→ 缓存兜底（热点问答）→ 静态 FAQ 兜底。每一级都是自动触发、自动恢复。

4. **Text-to-SQL 必须包含 SQL Guard 层** — 语法校验 + DDL/DML 拦截 + 只读副本执行 + 自修复循环。生产环境中不允许任何写操作风险。

5. **三源平等支柱** — PRD 文档、代码、SQL 是三个同等重要的检索入口。用户坚持了这一点（在第二意见挑战后维护了原始立场），因为跨源溯源（"这个需求影响了哪些代码和哪些表？"）是最核心的使用场景。

## Cross-Model Perspective

独立 AI 审查（Claude 子 Agent）提供了以下视角：

**Steelman（最强版本）：** "企业溯源引擎"——不是三个独立的搜索框，而是一个理解"需求如何变成代码、代码如何触及数据"的统一查询层。PM 问"这个 PRD 变更影响了哪些表？"，开发问"这个 bug 关联哪个需求的哪个版本？"——这些跨源问题才是 RAG 独有的价值。

**被挑战的前提：** 第二意见认为 Supervisor-Worker 对第一版过度复杂，建议先用分类器+顺序管道，等日志显示 >40% 查询需要跨源时再升级。用户的回应：坚持原始前提——因为跨源查询就是核心场景，从一开始就需要 Supervisor 编排。

**48 小时原型建议：** Text-to-SQL + PRD 上下文（跳过代码搜索），用 PGVector + BGE-M3 + Streamlit。正则表达式 SQL Guard 足够。零降级。

**综合：** 第二意见在"从 SQL 开始"这个方向上是正确的——SQL 是最大公约数。但它对 Supervisor 模式的反对在用户的场景下不成立，因为跨源查询不是边缘情况而是核心价值主张。

## Approaches Considered

### Approach A: Supervisor-Worker Agentic RAG ✅ 选中
**Summary:** 智能路由层理解意图 → 分发到三个专门化 Worker（文档/代码/SQL 各自用最优检索策略）→ 跨源合成层汇总。支持多跳推理、4 级降级、完整可观测性。
**Effort:** 人：5-7.5 人·月（2-3 人并行约 3-5 自然月）；AI 辅助可加速代码生成但无法缩短架构决策和数据源对接时间
**Risk:** 中（Agent 编排的复杂性需要精心设计 fallback）
**Pros:**
- 每种数据源用最优检索策略，不妥协
- 天然支持跨源多跳查询（核心价值场景）
- 易于扩展第四种、第五种数据源
- 业界验证的成熟模式（Protocol-H, LangGraph Supervisor）
**Cons:**
- Agentic 编排增加延迟和工程复杂度
- 需要精心设计回退路径和超时处理
- 调试和可观测性要求高于简单管道

### Approach B: 联邦并行检索 RAG
**Summary:** 不做智能路由——每个查询同时发到三个专门化检索管道，各自独立返回，用 RRF（Reciprocal Rank Fusion）融合排序后统一展示。
**Effort:** 人：~2-3 个月 / AI 辅助：~1-2 周
**Risk:** 低
**Pros:**
- 延迟更低（并行执行，无路由决策开销）
- 架构简单，调试容易
- 每条管道独立优化、独立故障隔离
**Cons:**
- 不支持多跳跨源推理（"先查文档再决定查哪个表"）
- 简单查询也要跑三条管道，资源浪费
- 无法动态调整检索策略

### Approach C: 知识图谱 + RAG 混合
**Summary:** 构建统一知识图谱——PRD 需求节点、代码模块节点、数据库表节点用关系边连接。RAG 做初始检索，图遍历做关系追踪。
**Effort:** 人：~4-6 个月 / AI 辅助：~3-4 周
**Risk:** 高
**Pros:**
- 最强的跨源溯源能力（确定性关系查询，不会幻觉）
- 对"影响分析"类问题最优
- 图结构天然支持版本演化和时间线查询
**Cons:**
- 构建和维护知识图谱成本极高
- 需要持续同步三种数据源的变化（CDC 链路）
- 冷启动问题（初始图谱构建工作量大）

## Recommended Approach: Supervisor-Worker Agentic RAG

### 总体架构

```
                            ┌─────────────────────────┐
                            │     API Gateway / LB     │
                            │   (限流、鉴权、审计日志)    │
                            └───────────┬─────────────┘
                                        │
                            ┌───────────▼─────────────┐
                            │    Supervisor Agent      │
                            │  ┌───────────────────┐   │
                            │  │ Query Understanding │  │
                            │  │  - 意图分类           │  │
                            │  │  - 实体抽取           │  │
                            │  │  - 查询改写           │  │
                            │  └───────┬───────────┘   │
                            │          │               │
                            │  ┌───────▼───────────┐   │
                            │  │ Task Decomposition │  │
                            │  │  - 单源 vs 跨源决策  │  │
                            │  │  - Worker 调度       │  │
                            │  │  - 依赖管理           │  │
                            │  └───────────────────┘   │
                            └───┬───────┬───────┬─────┘
                                │       │       │
                    ┌───────────▼┐ ┌────▼──────┐ ┌▼───────────┐
                    │ Doc Worker │ │Code Worker│ │ SQL Worker  │
                    │            │ │           │ │             │
                    │ BM25+向量  │ │ AST感知   │ │ Schema感知  │
                    │ 混合检索    │ │ 结构化搜索 │ │ RAG检索     │
                    │            │ │           │ │             │
                    │ BGE-M3     │ │ AST grep  │ │ SQL Guard   │
                    │ 嵌入模型    │ │ 结构化搜索  │ │ 自修复循环   │
                    └─────┬──────┘ └─────┬─────┘ └──────┬──────┘
                          │              │              │
                    ┌─────▼──────────────▼──────────────▼─────┐
                    │           Synthesis Layer               │
                    │  ┌──────────────────────────────────┐   │
                    │  │ 结果融合 · 去重 · 排序 · 溯源标注   │   │
                    │  └──────────────────────────────────┘   │
                    │  ┌──────────────────────────────────┐   │
                    │  │ LLM 生成（带引用来源的最终回答）     │   │
                    │  └──────────────────────────────────┘   │
                    └────────────────────┬────────────────────┘
                                         │
                    ┌────────────────────▼────────────────────┐
                    │           降级 & 容灾层                   │
                    │  L1: 全功能 → L2: LLM切换备用模型        │
                    │  L3: 向量降级 → L4: 缓存兜底 → L5: 静态FAQ │
                    └─────────────────────────────────────────┘
```

> 详细设计参见：
> - [Supervisor Agent（意图分类、实体抽取、查询改写）](SPMA-design-01-supervisor-agent.md)
> - [Doc Worker](SPMA-design-02-doc-worker.md)
> - [Code Worker](SPMA-design-03-code-worker.md)
> - [SQL Worker](SPMA-design-04-sql-worker.md)

## 跨源多跳查询 Trace 示例

用户问题：**"用户登录模块的 PRD 需求改了哪些内容？影响了哪些代码文件和数据库表？"**

```
Step 1: Supervisor — Query Understanding
  ├─ 意图分类: cross-source (文档 + 代码 + SQL)
  ├─ 实体抽取: {module: "用户登录", actions: ["PRD变更", "代码影响", "数据库影响"]}
  ├─ 输出: 独立 Worker 调用（Code 和 SQL 共享 Doc Worker 输出作上下文，但各自独立检索）
  └─ [延迟预算: 200ms]

Step 2: Doc Worker — 检索相关 PRD 文档
  ├─ BM25 查询: "用户登录" + "PRD" + "需求变更"
  ├─ 向量查询: BGE-M3 语义检索 "登录模块需求规格"
  ├─ RRF 融合 → Top 5 文档片段
  ├─ LLM 提取: 涉及的改动点列表、需求 ID (如 REQ-2024-0187)
  └─ 返回: {req_ids: ["REQ-2024-0187"], changes: [{desc: "增加OAuth2.0登录", ...}, ...]}
        [延迟预算: 1500ms]

Step 3: Code Worker & SQL Worker — 并行执行
  ├─ Code Worker:
  │   ├─ 输入: {req_ids: ["REQ-2024-0187"], module: "用户登录"}
  │   ├─ Repo Router: "用户登录" → ["auth-service", "auth-gateway"]
  │   ├─ AST 感知搜索: grep commit log + 关键词搜索 "REQ-2024-0187" OR "oauth" OR "login"
  │   ├─ 元数据过滤: repo IN ('auth-service','auth-gateway') + 文件路径包含 "auth/" OR "login/"
  │   └─ 返回: [{file: "src/auth/oauth.py", funcs: ["login_oauth", "token_refresh"]}, ...]
  │        [延迟预算: 200ms]
  │
  └─ SQL Worker (与 Code Worker 并行，不依赖 Code 输出):
      ├─ 输入: {req_ids: ["REQ-2024-0187"], module: "用户登录"}
      ├─ Schema RAG: 检索 "user auth token oauth" 相关表名和列名
      ├─ 生成验证 SQL: SELECT table_name FROM information_schema... （验证表存在）
      └─ 返回: [{table: "users", cols: ["id","email","oauth_provider","oauth_token"]},
                {table: "user_sessions", cols: [...]}, {table: "oauth_states", cols: [...]}]
           [延迟预算: 1500ms]

  → Code & SQL 并行执行，总耗时: max(1200, 1500) = 1500ms

Step 4: Synthesis — 汇总生成回答
  ├─ 输入: Doc结果 + Code结果 + SQL结果
  ├─ RRF 去重融合（跨源结果归一化排序）
  ├─ LLM 生成结构化回答（含引用标注: [PRD §3.2], [src/auth/oauth.py:42], [users 表]）
  └─ 返回给用户
        [延迟预算: 1500ms]

总延迟预算: 200(路由) + 1500(Doc) + 1500(Code∥SQL) + 1500(Synthesis) = 4700ms
加上网络 RTT (3-4 hop × ~10ms) ≈ 4740ms
目标: P95 < 5000ms ✓  |  P99 < 8000ms（允许 LLM 尾延迟）
```

## 阶段性交付路线图

| Phase | 交付内容 | 用户价值 | 时间（人·月） |
|-------|---------|---------|------------|
| **Phase 1** | SQL Worker（Text-to-SQL + SQL Guard + 自修复）+ 基本降级 L0-L1 | PM 和开发都能用自然语言查数据 | 1.5-2 月 |
| **Phase 2** | Doc Worker（PRD 文档混合检索）+ 单源查询 + Redis 缓存 | PM 可以检索和对比历史 PRD 文档 | +1-1.5 月 |
| **Phase 3** | Code Worker（AST 感知代码搜索）+ Supervisor 跨源编排 | 开发可以追溯需求→代码→数据的影响链 | +1.5-2 月 |
| **Phase 4** | 完整降级 L0-L4 + 熔断器 + K8s 蓝绿部署 + 混沌工程演练 | 企业级可靠性达标（99.9%） | +1-2 月 |

**总时间线: 5-7.5 人·月**（2-3 人团队并行约 3-5 个自然月）

每个 Phase 独立交付价值、独立回滚、独立验收。Phase 1 就是"这周能跑起来"的版本；Phase 4 是"企业级完整版"。

## Success Criteria

- **检索质量：** Recall@10 ≥ 0.85（跨三种数据源；Ground Truth 来自 Shadowing 收集的 50 条真实用户问题，由领域专家标注正确答案）
- **SQL 准确率：** Text-to-SQL 端到端 Execution Accuracy ≥ 80%（含自修复循环；评估集：50 条真实业务问题 + 对应正确 SQL）
- **响应延迟：** 
  - 单源查询 P99 < 3 秒
  - 跨源查询 P95 < 5 秒、P99 < 8 秒（LLM 调用的 P99 尾延迟不可控，需预留余量）
- **可用性：** 系统可用性 ≥ 99.9%（含降级路径；月度统计）
- **用户满意度：** 内部用户 NPS ≥ 30（上线后 3 个月收集）
- **知识新鲜度：** 文档/代码变更到可检索 < 5 分钟（通过 Git Webhook / CI 触发增量索引）；数据库 Schema 变更 < 10 分钟（定时轮询 + 手动触发刷新）
- **Text-to-SQL 准确率：** v1 目标 Execution Accuracy ≥ 65%（含自修复循环），v2 通过优化提升到 ≥ 80%

## Open Questions

1. **PRD 文档存储位置和格式？** Confluence、飞书文档、本地 Markdown？是富文本还是纯文本？不同来源的解析器选择不同。
2. **代码仓库规模和语言？** 单仓还是多仓？主要语言（Python/Java/Go/JS）？代码 commit 中是否有关联需求 ID（如 `[REQ-1234]`）的元数据？这决定了 Code Worker 的 AST 解析策略和跨源关联的可行性。
3. **SQL 数据库类型和规模？** PostgreSQL、MySQL、还是多种？表的数量级（几十 vs 几千张表？）、是否有 query log 可作为 few-shot 示例来源？
4. **权限模型（v2）？** 是否需要按用户角色限制可查询的数据范围？（PM 不能查薪资表、开发不能查客户隐私数据等）v1 先做全员统一权限，v2 引入 RBAC。
5. **中文→英文代码标识符映射质量？** Code Worker 不依赖代码 embedding，而是通过同义词映射表 + LLM 辅助翻译将中文查询转为英文标识符 grep。需要评估这套映射机制覆盖了多少真实查询场景。Phase 3 前需完成此项评估。

## Dependencies

- **数据源接入：** Confluence/Wiki API、Git 仓库访问权限、数据库只读副本
- **LLM 服务：** Claude API（通过数据脱敏层） + 本地 Qwen3-32B（vLLM 部署，高敏感查询和无外网时使用）
- **基础设施：** K8s 集群、PostgreSQL + pgvector、Redis
- **人员：** 后端工程师 1-2 人、算法/NLP 工程师 1 人（RAG 调优）、前端工程师 0.5 人（Streamlit/Gradio 即可）

## The Assignment

在开始写代码之前，做三件事：

1. **影子观察（Shadowing）：** 花一个下午坐在一个产品经理和一个开发工程师旁边，看他们实际怎么查文档、搜代码、查数据。记录具体操作序列和耗时。这会在你设计 UI 和工作流时反复提醒你真实的使用模式，而不是你想象中的模式。

2. **数据源盘点：** 整理三种数据源的具体清单——PRD 文档存在哪（Confluence/飞书/其他）、代码仓库数量和语言（Python/Java/Go）、数据库类型和规模（PG/MySQL、表数量级）。这直接决定解析器选型和索引策略。

3. **搭建 Text-to-SQL 原型：** 用 PGVector + BGE-M3 + Claude API + Streamlit，先跑通一个只做 SQL 自然语言查询的最小原型。让一个真实的 PM 同事用它查一次数据。你看他们第一反应就知道这条路对不对。

## What I noticed about how you think

- 当你说"不仅仅是简单技术方案，要完整的企业级落地"时，你展示了对质量的执着——这不是一个"我先做个 demo 试试"的心态，而是"我要做一个同事真正依赖的系统"的承诺。
- 你坚持三源平等支柱、推回了第二意见的挑战——不是固执，而是你比任何外部审查者都更清楚跨源溯源是你的核心场景。这种"我知道用户要什么"的判断力值得留意。
- 你选的用户群体（PM + 开发）不是随机组合——这两个角色覆盖了产品从需求到实现的完整价值链。这说明你对组织运作方式有真实的理解，不是在抽象地思考"知识管理"。
- 你从三种数据源出发而不是从技术方案出发——这是产品思维，不是技术炫技。先定义"用户需要从哪获取信息"，再设计"怎么获取"。正确顺序。
